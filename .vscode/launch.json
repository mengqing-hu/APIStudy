{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug active file (lldb + clang++)",
        "type": "lldb",
        "request": "launch",
        "program": "${fileDirname}/${fileBasenameNoExtension}",
        "args": [],
        "cwd": "${fileDirname}",
        "preLaunchTask": "C/C++: clang++ build active file (C++17 + nlohmann)",
        "console": "integratedTerminal"
      }
    ]
  }
  